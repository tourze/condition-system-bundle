services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: false

  # DataFixtures 只在测试环境中加载
  Tourze\ConditionSystemBundle\DataFixtures\:
    resource: '../../DataFixtures'
    tags: ['doctrine.fixture.orm']